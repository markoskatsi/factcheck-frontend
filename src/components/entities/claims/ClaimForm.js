const emptyClaim = {
  ClaimTitle: "",
  ClaimDescription: "",
  ClaimStatus: "",
  ClaimCreated: 0,
  ClaimUserID: 0,
  ClaimClaimstatusID: 0,
};

export default function ClaimForm({initialClaim=emptyClaim}) {
  // Initialisation --------------------------------
  // State -----------------------------------------
  // Handlers --------------------------------------
  // View ------------------------------------------
  return <div>Claim Form Component</div>;
}
