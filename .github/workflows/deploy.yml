name: Deploy Frontend

on:
  push:
    branches:
      - master

jobs:
  deploy:
    runs-on: self-hosted

    steps:
      - name: Run deploy script
        run: ~/projects/factcheck-frontend/deploy.sh
